# VRAXION core dependencies
# Install: pip install -r requirements.txt
torch>=2.1
numpy>=1.24

# Optional: only needed for MNIST / real-data modes
# torchvision>=0.16

# Live dashboard (Streamlit UI)
streamlit>=1.30
plotly>=5.18
pandas>=2.0
