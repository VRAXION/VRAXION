[package]
name = "instnct-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Core spiking network engine: quaternary mask, forward pass, evolution, crystal"

[dependencies]
rand = "0.8"
rayon = "1.10"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "forward_bench"
harness = false
