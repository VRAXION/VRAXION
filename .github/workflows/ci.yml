name: CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  rust-runtime-ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo artifacts
        uses: Swatinem/rust-cache@v2

      - name: Rust format
        run: cargo fmt --check -p vraxion-runtime

      - name: Rust clippy
        run: cargo clippy -p vraxion-runtime --all-targets -- -D warnings

      - name: Rust runtime tests
        run: cargo test -p vraxion-runtime

      - name: Final bake smoke with progress artifacts
        run: |
          cargo run --release -p vraxion-runtime --bin final_bake_preflight -- 1000 target/ci/e75_final_bake_smoke
          test -f target/ci/e75_final_bake_smoke/progress.jsonl
          test -f target/ci/e75_final_bake_smoke/final_bake_results.json
          test -f target/ci/e75_final_bake_smoke/report.md

      - name: Final curriculum runner smoke with progress artifacts
        run: |
          cargo run --release -p vraxion-runtime --bin final_training_runner -- 1000 target/ci/e75_final_training_smoke --preflight-rounds 100 --checkpoint-interval 100
          test -f target/ci/e75_final_training_smoke/progress.jsonl
          test -f target/ci/e75_final_training_smoke/checkpoint_latest.json
          test -f target/ci/e75_final_training_smoke/partial_summary.json
          test -f target/ci/e75_final_training_smoke/final_training_results.json
          test -f target/ci/e75_final_training_smoke/library_summary.json
          test -f target/ci/e75_final_training_smoke/report.md
