"""Historical root-level exploratory probes archived from the active tests root.

These files were previously visible directly under `v4.2/tests/` but had no
required place in the current mainline keep-set during the open-beta cleanup
pass.

They remain available as historical exploratory material only. They are not
treated as active first-class entrypoints, CI surfaces, or current-line
recommended probes.
"""
