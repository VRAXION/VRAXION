# STABLE_LOOP_PHASE_LOCK_133_TARGETED_POST_INJECTION_REPAIR_OR_SCALE_PLAN_RESULT

## Result Contract

The 133 runner writes generated artifacts under:

```text
target/pilot_wave/stable_loop_phase_lock_133_targeted_post_injection_repair_or_scale_plan/
```

The expected positive result is:

```text
TARGETED_POST_INJECTION_REPAIR_OR_SCALE_PLAN_POSITIVE
selected_next_milestone = 134_STRUCTURED_OUTPUT_TOOL_API_REPAIR
selected_repair_target = structured_output_tool_api_first
```

The result must include:

- post-injection failure priority map
- breakpoint analysis
- root-vs-symptom analysis
- repair target selection
- training design options
- eval gate proposal
- risk register
- 111 failure prevention map
- concrete 134 next milestone plan

The 134 plan must distinguish exact JSON object generation,
schema-constrained object generation, tool/API argument generation, format
conversion without semantic drift, structured refusal envelopes,
injection-resistant structured fields, and safe structured answers when rows
are answerable.

The 134 plan must include raw-only final eval, no oracle rerank, no decoder
reference, no expected-answer metadata, no verifier rerank, no LLM judge, and no
teacher-forcing-only success. A repair that only learns a fixed JSON template,
validates schema while emitting wrong values, preserves values while breaking
schema, copies injected text into structured fields, fakes tool results, or
improves by over-refusing must fail.

## Boundary

133 is planning only. 134 is planned as research repair only. It is not
GPT-like assistant readiness, not open-domain assistant readiness, not
production chat, not public API, not deployment readiness, not safety alignment,
and not Hungarian assistant readiness.
