# D104 Sparse Recurrent Generalization and Compression Frontier Map Contract

## Purpose

D104 maps the generalization and compression frontier of the D103 scale-confirmed 8% light-pressure protected sparse recurrent routing microcircuit. It does not increase the fair healthy sparse target beyond 8%, does not run production pruning, and does not use moderate/aggressive or reference-only sparse arms as healthy evidence. The run asks whether the D103 sparse recurrent core transfers across additional controlled symbolic formula-discovery families, which families expose brittleness, whether the sparse mask/checkpoint remains stable under family shifts, and whether D105 cross-family train-loop integration is justified.

## Boundary

D104 is controlled symbolic formula-discovery generalization only. The formula solver remains symbolic. D104 does not use raw visual Raven tasks, natural-language pretraining, or Gemma-class model training. It maps generalization/compression frontier only and does not claim a full VRAXION brain, raw visual Raven solved, AGI, consciousness, DNA/genome success, production readiness, or architecture superiority.

## Phase 0 upstream audit

The runner must verify branch/HEAD, check D103 commit `84290ac5e667b7acc35cd9beb2f15f239cc8acd1`, verify `target/pilot_wave/d103_sparse_recurrent_microcircuit_scale_confirm/`, validate D103 decision `d103_sparse_recurrent_microcircuit_scale_confirmed` with next `D104_SPARSE_RECURRENT_GENERALIZATION_AND_COMPRESSION_FRONTIER_MAP`, confirm `d104_ready=true`, final sparse candidate `D102_SPARSE_AUTO_ANNEAL_8PCT_LIGHT_PROTECTED_FAIR`, final sparse percent `8`, final pressure `light`, restore/rerun D103 if required, and write `d103_upstream_manifest.json`. D104 must not silently assume D103 was pushed.

## Scale, families, and stress settings

Requested main scale is seeds `25001,25002,25003,25004,25005,25006,25007,25008,25009,25010` with rows `train/test/ood=560` per seed/regime/split. Requested family extension is seeds `25101,25102,25103,25104,25105,25106,25107,25108` with rows `train/test/ood=520` per seed/family/regime/split across 14 controlled symbolic families. Requested stress extension is seeds `25201,25202,25203,25204,25205,25206`, stress rows `720`, and D103 sparse/recurrent stresses plus cross-family transfer, heldout family, family calibration, family loop utility, family mask stability, and family compression tails. Any scale reduction must be recorded and cannot overclaim.

## Controlled symbolic family set

The family set is `ECF_IPF_BASE_REPLAY`, `ECF_IPF_HELDOUT_OPERATOR_MIX`, `ECF_IPF_HELDOUT_COMPOSITION_DEPTH`, `LOW_COST_OOD_TOP1_AMBIGUITY_FAMILY`, `JOINT_REQUIRED_BOUNDARY_FAMILY`, `OOD_SUPPORT_SHIFT_FAMILY`, `EXTERNAL_REQUIRED_FAMILY`, `INDISTINGUISHABLE_ABSTAIN_FAMILY`, `CORRELATED_ECHO_DISTRACTOR_FAMILY`, `ADVERSARIAL_COUNTER_FAMILY`, `POLYNOMIAL_SYMBOLIC_COMPOSITION_FAMILY`, `RATIONAL_SYMBOLIC_COMPOSITION_FAMILY`, `TRIG_PERIODIC_SYMBOLIC_FAMILY`, and `MIXED_SYMBOLIC_TRANSFER_FAMILY`. These remain controlled symbolic formula-discovery families only.

## Required reports

Artifacts are written under `target/pilot_wave/d104_sparse_recurrent_generalization_and_compression_frontier_map/` and must include `d103_upstream_manifest.json`, scale, family set, sparse candidate identity, dense baseline reference, sparse candidate generalization map, cross-family/heldout/calibration/family-specific reports, transfer-family reports, compression frontier reports, sparse mask/checkpoint/recovery reports, protected/candidate component reports, oracle/support reports, sentinel reports, split/overfit/truth-oracle/schema/deterministic reports, `aggregate_metrics.json`, `decision.json`, `summary.json`, and `report.md`.

## Positive gate

D104 passes only if D103 handoff/replay is valid, requested and actual scale are recorded with `scale_reduced=false`, all stress modes and requested families execute, sparse identity remains 8% light protected, protected components remain locked, checkpoint/mask replay and recovery pass, family failures are visible rather than hidden, family pass-rate and heldout-transfer thresholds pass, aggregate sparse generalization accuracy/tail/loop/halting/top1/D68/truth/oracle/Rust/support/cost gates pass, reference-only arms are not used to overclaim, leak/shortcut sentinels collapse, controls remain worse, deterministic replay/schema/metric crosscheck pass, `fallback_rows=0`, and `failed_jobs=[]`.

## Decisions

- Healthy frontier mapped: `decision=d104_sparse_recurrent_generalization_frontier_mapped`, `next=D105_CROSS_FAMILY_TRAIN_LOOP_INTEGRATION_PLAN`.
- Heldout family gap: `decision=d104_heldout_family_generalization_gap_detected`, `next=D104H_HELDOUT_FAMILY_REPAIR_PLAN`.
- Compression frontier fragility: `decision=d104_compression_frontier_fragility_detected`, `next=D104C_COMPRESSION_FRONTIER_REPAIR`.
- Family-specific tail risk: `decision=d104_family_specific_tail_risk_detected`, `next=D104T_FAMILY_SPECIFIC_TAIL_REPAIR`.
- Loop utility transfer failure: `decision=d104_loop_utility_transfer_failure`, `next=D104L_LOOP_UTILITY_TRANSFER_REPAIR`.
- Sparse mask/checkpoint family failure: `decision=d104_sparse_mask_checkpoint_family_failure`, `next=D104M_MASK_CHECKPOINT_FAMILY_REPAIR`.
- Guard/safety regression: `decision=d104_guard_or_safety_regression_detected`, `next=D104G_GUARD_SAFETY_REPAIR`.
- Truth/oracle/family leak: `decision=d104_truth_or_family_leak_detected`, `next=D104L_LEAK_REPAIR`.
- Shortcut memorization: `decision=d104_shortcut_memorization_detected`, `next=D104S_SHORTCUT_MEMORIZATION_REPAIR`.
- Rust fallback: `decision=d104_rust_fallback_detected`, `next=D104R_RUST_PATH_REPAIR`.
- Reporting inconsistency: `decision=d104_invalid_metric_or_report_inconsistency`, `next=D104_REPORTING_REPAIR`.
- Incomplete run: `decision=d104_invalid_or_incomplete_run`, `next=D104_RETRY_WITH_FULL_AUDIT`.
