# STABLE_LOOP_PHASE_LOCK_140HS_INSTNCT_POCKET_GATED_MINIMAL_MARKER_REAL_TASK_BRIDGE_SCALE_CONFIRM

140HS is the single follow-up after a positive 140H result. It scale-confirms
the minimal-marker real-task bridge over more seeds, families, rows, scaffold
styles, controls, and ablations.

The milestone must not train, mutate source checkpoints, modify
`shared_raw_generation_helper.py`, import old phase runners, deploy, alter
runtime/release/product surfaces, change root `LICENSE`, or make public API
changes. Final generation remains helper-only.

Positive requires:

- >= 2000 eval rows
- >= 6 families
- >= 40 scaffold variants
- main answer value accuracy >= 0.95
- main pocket writeback rate >= 0.95
- main phase transport success rate >= 0.95
- main contrast group accuracy >= 0.95
- ablation answer value accuracy <= 0.05
- ablation pocket writeback rate <= 0.02
- pocket ablation delta >= 0.90
- direct `POCKET_VALUE=` marker rate = 0.0
- explicit `POCKET_` token row rate <= 0.10
- implicit/minimal gate row rate >= 0.90
- every seed independently passes
- visible bypass and noisy distractor controls fail
- expected-output canary, AST scan, leakage audit, controls, generated-before-scoring, and deterministic replay pass

Expected positive route:

`instnct_pocket_gated_minimal_marker_real_task_bridge_scale_confirmed -> 140HT_INSTNCT_POCKET_GATED_REAL_TASK_TRANSFER_DECISION_PLAN`

This remains constrained pocket-gated helper evidence, not GPT-like readiness,
not broad assistant capability, not production readiness, not public API
readiness, not deployment readiness, and not safety alignment.
