# STABLE_LOOP_PHASE_LOCK_139YS_INSTNCT_POCKET_GATED_MUTATION_SEARCH_SCALE_OR_REAL_TASK_BRIDGE Contract

139YS is a planning-only decision milestone after 139YR.

It reads existing 139YR, 139YQ, 138YO, and 138YK artifacts, summarizes the evidence chain, compares more marker-bound scale against a reduced-marker bridge, and emits the next milestone recommendation.

## Boundary

139YS does not train, run new inference, call `shared_raw_generation_helper.py` for generation, mutate checkpoints, modify helper/backend/runtime/release/product surfaces, import old phase runners, start services, deploy, change root `LICENSE`, or claim GPT-like readiness.

139YS must keep false:

- `reasoning_restored`
- `raw_assistant_capability_restored`
- `structured_tool_capability_restored`
- `gpt_like_readiness_claimed`
- `open_domain_assistant_readiness_claimed`
- `production_chat_claimed`
- `public_api_claimed`
- `deployment_readiness_claimed`
- `safety_alignment_claimed`

## Required Upstreams

139YS requires:

- 139YR mutation search confirmed, `gradient_used = false`, selected `open_pocket_all_payload_markers`, `fitness_margin >= 0.40`, and next `139YS_INSTNCT_POCKET_GATED_MUTATION_SEARCH_SCALE_OR_REAL_TASK_BRIDGE`.
- 139YQ scale confirm with main answer value accuracy `1.0`, pocket writeback `1.0`, phase transport `1.0`, ablation answer value accuracy `0.0`, ablation delta `1.0`, and deterministic replay passed.
- 138YO adapter improvement over byte-GRU with pocket writeback `0.0` and pocket ablation delta `0.0`.
- 138YK byte-GRU value accuracy `0.0` with family-default shortcut still detected and deterministic replay passed.

If these are incomplete or inconsistent, 139YS routes to:

```text
decision = upstream_evidence_incomplete
next = 139YS_UPSTREAM_EVIDENCE_REPAIR
```

## Required Outputs

Generated outputs stay under:

```text
target/pilot_wave/stable_loop_phase_lock_139ys_instnct_pocket_gated_mutation_search_scale_or_real_task_bridge/
```

Required artifacts:

- `queue.json`
- `progress.jsonl`
- `analysis_config.json`
- `upstream_139yr_manifest.json`
- `upstream_139yq_manifest.json`
- `upstream_138yo_manifest.json`
- `upstream_138yk_manifest.json`
- `evidence_chain_summary.json`
- `scale_vs_bridge_decision_matrix.json`
- `risk_register.json`
- `next_milestone_recommendation.json`
- `target_140a_milestone_plan.json`
- `decision.json`
- `summary.json`
- `report.md`

## Decision

Expected complete decision:

```text
decision = real_task_bridge_recommended
next = 140A_INSTNCT_POCKET_GATED_NOISY_MARKER_BRIDGE_PROBE
```

The recommendation is a short-term bridge because the current marker-bound pocket proof has already passed scale confirmation and mutation-search selection. The next useful falsification is whether pocket-gated value binding survives weaker explicit markers and noisier task-like prompt text.

## Target 140A Requirements

The generated 140A plan must require:

- reduced explicit `POCKET_VALUE` marker scaffolding
- noisy prompt distractors
- value hidden behind natural-ish task text
- pocket gate still required
- visible value bypass forbidden
- closed-pocket ablation failure
- mutation selection still preferring the correct open-pocket config
- helper-only final eval
- expected-output canary, AST scan, leakage audit, controls, and deterministic replay

140A positive gates must include:

- main answer value accuracy at least `0.80`
- pocket writeback at least `0.90`
- ablation answer value accuracy at most `0.10`
- pocket ablation delta at least `0.50`
- visible bypass control fails
- noisy distractor control fails
- deterministic replay passes

Clean-negative routes must include marker dependency, pocket causality failure, noisy prompt value-binding failure, mutation selection failure, and helper integrity failure.

This is not GPT-like readiness, not broad assistant capability, not production readiness, not public API readiness, not deployment readiness, and not safety alignment.
