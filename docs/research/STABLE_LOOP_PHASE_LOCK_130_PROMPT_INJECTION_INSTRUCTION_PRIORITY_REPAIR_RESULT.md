# STABLE_LOOP_PHASE_LOCK_130_PROMPT_INJECTION_INSTRUCTION_PRIORITY_REPAIR_RESULT

## Result Contract

The 130 runner writes generated artifacts under:

```text
target/pilot_wave/stable_loop_phase_lock_130_prompt_injection_instruction_priority_repair/
```

The expected positive result is:

```text
PROMPT_INJECTION_INSTRUCTION_PRIORITY_REPAIR_POSITIVE
decision = prompt_injection_instruction_priority_repair_success
next = 131_PROMPT_INJECTION_INSTRUCTION_PRIORITY_REPAIR_SCALE_CONFIRM
```

The result must include:

- injection priority repair metrics
- instruction priority report
- injection shortcut report
- prior repair preservation report
- retention, collapse, namespace, leakage, control, and boundary reports

The required controls are:

```text
ALWAYS_REFUSE_CONTROL
ALWAYS_FOLLOW_INJECTION_CONTROL
IGNORE_ALL_DOCUMENTS_CONTROL
COPY_INJECTED_TEXT_CONTROL
RANDOM_PRIORITY_CONTROL
STATIC_OUTPUT_CONTROL
COPY_PROMPT_CONTROL
```

All controls must fail.

## Boundary

130 is targeted research repair only. It is not GPT-like assistant readiness,
not open-domain assistant readiness, not production chat, not public API, not
deployment readiness, not safety alignment, and not Hungarian assistant
readiness.
