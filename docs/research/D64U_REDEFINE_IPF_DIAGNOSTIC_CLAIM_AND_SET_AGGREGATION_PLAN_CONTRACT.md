# D64U Redefine IPF Diagnostic Claim And Set Aggregation Plan Contract

## Question

D64/D64B/D64S/D64T changed the correct claim boundary. D64U asks what is
actually supported now, what must be rejected or downgraded, and what D65 should
test next.

## Inputs

D64U reads existing artifacts if present:

```text
D64  = Rust sparse IPF diagnostic layer prototype
D64B = diagnostic calibration and shuffle hardening
D64S = score-vector structure repair
D64T = temporal support trajectory audit
```

It does not recreate those runs and does not invent missing metrics.

## Supported Claim Boundary

Supported:

```text
Rust sparse IPF diagnostic layer is controller-useful in the controlled task.
Clean D63 proxy flags were not used as Rust estimator inputs in D64.
Candidate identity dependence is not confirmed.
Temporal order/trajectory is not required in the current task.
Set-invariant support/evidence aggregation is the best next candidate for D65.
```

Rejected or unconfirmed:

```text
candidate score-vector identity is essential
support temporal order is essential
all diagnostic bits are calibrated
full IPF aggregation migrated
full VRAXION brain / raw Raven / AGI / consciousness / architecture superiority
```

## Artifacts

Generated artifacts live under:

```text
target/pilot_wave/d64u_redefine_ipf_diagnostic_claim_and_set_aggregation_plan/
```

Required reports:

```text
claim_boundary_report.json
supported_claims_report.json
rejected_or_unconfirmed_claims_report.json
d65_set_aggregation_plan.json
aggregate_metrics.json
decision.json
summary.json
report.md
```

## Decision

If the upstream artifacts are present and the claim repair is complete:

```text
decision = ipf_diagnostic_claim_redefined_set_aggregation_ready
next = D65_SET_INVARIANT_IPF_AGGREGATION_PROTOTYPE
```

If upstream evidence is missing or contradictory:

```text
decision = d64u_upstream_incomplete
next = D64U_REPAIR
```

## Boundary

D64U only repairs the IPF diagnostic claim and prepares set-invariant
aggregation planning in controlled symbolic joint formula discovery. It does
not prove full VRAXION brain, raw visual Raven reasoning, Raven solved, AGI,
consciousness, DNA/genome success, architecture superiority, or production
readiness.
