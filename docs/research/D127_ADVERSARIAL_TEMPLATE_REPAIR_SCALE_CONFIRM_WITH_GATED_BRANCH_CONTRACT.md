# D127 Adversarial Template Repair Scale Confirm with Gated Branch Contract

Purpose: scale-confirm the D126 adapter-only adversarial-template overlap repair and verify that the gated multi-correction branch remains better than standard weighted-sum repair at larger scale.

Boundary: D127 is controlled symbolic adversarial-template repair scale confirmation only. It preserves the symbolic formula solver, dense baseline, protected symbolic router, protected components, recurrent base paths, and D102 8% light sparse mask. It performs no natural-language pretraining, introduces no tokenizer or next-token objective, uses no raw text corpus or raw Raven tasks, and does not train a Gemma-class model.

Upstream: D126 must replay or restore as d126_adversarial_template_repair_prototype_confirmed_gated_branch with d127_ready=true, selected_branch=gated_multi_correction, gated_branch_wins=true, adversarial/template/grammar/route reductions intact, guarded/reference family policies intact, preservation intact, sparse identity intact, fallback_rows=0, and failed_jobs=[].

Trainable surfaces: only halting_head_adapter_delta, route_head_adapter_delta, and calibration_scalar_adapter_delta are trainable. recurrent_state_adapter, symbolic solver, dense baseline, protected symbolic router, protected components, base recurrent path, base heads, sparse mask, and protected base paths remain frozen.

Scale branch policy: D127 scale-tests D127_GATED_MULTI_CORRECTION_SCALE_FAIR against D127_STANDARD_WEIGHTED_SUM_SCALE_REFERENCE plus route-priority, shortcut-suppression, calibration-gated, preservation-gated, combined-gated, and reference/control arms.

Family policy: TEMPLATE_NEAR_COLLISION_FAMILY and GRAMMAR_NEAR_COLLISION_FAMILY may remain guarded_low_weight only; adversarial-template overlap, same/different surface route, order perturbation, and binding-shadow families remain reference-only and excluded from healthy claims.

Positive gate: D127 passes only if the D126 handoff validates or restores, scale is not reduced, adapter-only scale training executes, gated-vs-standard comparison completes, adversarial failure and route uncertainty remain reduced at scale, gated remains non-regressive on shortcut and preservation risk, guarded/reference policies hold, preservation/sparse/Rust/D68/top1/leak gates hold, fallback_rows=0, failed_jobs=[], and report/metric crosschecks pass.

Decision target: d127_adversarial_template_repair_scale_confirmed_gated_branch -> D128_CONTROLLED_SYMBOLIC_BRIDGE_FRONTIER_CONSOLIDATION_PLAN when the gated branch scale-confirms and all gates pass.
