# STABLE_LOOP_PHASE_LOCK_138YL_INSTNCT_MUTATION_HELPER_INTEGRATION_ANALYSIS Contract

## Purpose

138YL is an artifact-only integration analysis after the 138YK byte-GRU clean negative and the runner-local highway/pocket mutation microprobe.

The core question is:

```text
Can the INSTNCT mutation/grower path be compared against the current byte-GRU raw-helper path under the same strict helper-only evidence rules?
```

This phase does not train, run helper inference, call `shared_raw_generation_helper.py`, run torch forward passes, mutate checkpoints, modify helper/backend/runtime/service/product/release surfaces, import old phase runners, delete files, deploy, or change root `LICENSE`.

## Required Inputs

- 138YK smoke artifacts from `target/pilot_wave/stable_loop_phase_lock_138yk_family_default_suppressed_contrastive_repair_probe/smoke`
- HIGHWAY_POCKET_MUTATION_001 artifacts from `target/pilot_wave/highway_pocket_mutation_001/current_micro_20260523` or an equivalent smoke run
- phase-lock 004 mutation credit assignment artifacts from `target/pilot_wave/stable_loop_phase_lock_004_mutation_credit_assignment/smoke_bounded`
- repo source files for `shared_raw_generation_helper.py`, `diag_highway_pocket_mutation.py`, `instnct-core/src/evolution.rs`, `instnct-core/src/network.rs`, and main grower/evolve examples

## Required Outputs

Generated outputs must stay under:

```text
target/pilot_wave/stable_loop_phase_lock_138yl_instnct_mutation_helper_integration_analysis/
```

Required artifacts:

```text
queue.json
progress.jsonl
analysis_config.json
upstream_138yk_manifest.json
mutation_microprobe_manifest.json
phase_lock_004_manifest.json
helper_backend_audit.json
instnct_mutation_surface_map.json
generator_contract_gap_analysis.json
gradient_vs_mutation_credit_report.json
external_research_manifest.json
integration_options.json
integration_risk_register.json
target_138ym_milestone_plan.json
decision.json
summary.json
report.md
```

## Analysis Requirements

138YL must verify that the current real-raw helper path is byte-LM based:

```text
HELPER_BACKEND = repo_local_checkpoint_byte_lm
ByteRNNLM / ByteMLPLM are the helper-loadable model families
138YK source checkpoint backend = byte_gru_lm
```

138YL must preserve the mutation evidence:

```text
HIGHWAY_POCKET_MUTATION_POSITIVE
MUTATION_RESCUES_PHASE_CREDIT_ASSIGNMENT
```

It must also preserve the stronger counter-evidence:

```text
PHASE_CREDIT_ASSIGNMENT_NOT_SOLVED
PHASE_TRANSPORT_IS_BLOCKER
```

The side-pocket mechanism must be described as gradient-free fitness selection:

```text
current graph -> score
mutate topology/gate/writeback -> score again
if better keep, otherwise rollback
```

No hidden-state, output-head, grower, scout, or topological mechanism may be claimed as measured unless an explicit artifact exists.

## Required Decision

If analysis completes:

```text
decision = instnct_mutation_helper_integration_analysis_complete
next = 138YM_INSTNCT_MUTATION_RAW_HELPER_ADAPTER_PLAN
```

The decision must say:

```text
instnct_helper_adapter_required = true
real_raw_value_grounding_comparison_ready = false
```

## Boundary Flags

All broad capability/readiness flags must remain false:

```text
reasoning_restored
reasoning_subtrack_real_raw_evidence_partially_restored
raw_assistant_capability_restored
structured_tool_capability_restored
gpt_like_readiness_claimed
open_domain_assistant_readiness_claimed
production_chat_claimed
public_api_claimed
deployment_readiness_claimed
safety_alignment_claimed
```

Raw assistant capability remains quarantined. Structured/tool capability remains invalidated. This is not GPT-like readiness, not open-domain assistant readiness, not production chat, not public API, not deployment readiness, and not safety alignment.
