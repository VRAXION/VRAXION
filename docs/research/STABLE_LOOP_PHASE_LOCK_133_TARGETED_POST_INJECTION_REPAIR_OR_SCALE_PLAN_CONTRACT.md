# STABLE_LOOP_PHASE_LOCK_133_TARGETED_POST_INJECTION_REPAIR_OR_SCALE_PLAN_CONTRACT

## Purpose

133 is planning only. It reads the positive 132 post-injection ceiling/gap
remap and writes the concrete 134 structured-output/tool-API repair plan. It
performs no training, no repair, no inference, no checkpoint mutation, no
service startup, and no deployment smoke.

Positive 133 means the 134 repair plan is evidence-linked, schema/tool focused,
shortcut-safe, and boundary-safe.

## Evidence

The selected target must be linked to 132:

```text
first_breakpoint_tier = TIER_5_STRUCTURED_OUTPUT_AND_TOOL_API_LIKE
first_breakpoint_family = structured_output_failure
primary_next_repair_target = structured_output_failure
structured_output_failure = 384
tool_api_schema_failure = 384
reasoning_preserved = true
state_preserved = true
calibration_preserved = true
injection_priority_preserved = true
unknown_failure_rate = 0.0
```

The plan must also cite later/global evidence without letting it override the
first breakpoint:

```text
structured_output_failure = 384
tool_api_schema_failure = 384
format_failure = 384
multi_doc_priority_failure = 192
long_context_failure = 192
```

First breakpoint outranks global failure count. Do not select long-context,
multi-doc ambiguity, or format-only first unless root-vs-symptom evidence proves
it is upstream of Tier 5 structured/tool/API failure.

## Required Decision

```text
selected_next_milestone = 134_STRUCTURED_OUTPUT_TOOL_API_REPAIR
selected_repair_target = structured_output_tool_api_first
```

The 134 draft must target structure plus semantics, not only valid JSON. It must
gate `json_validity_rate`, `schema_validity_rate`, `exact_key_match_accuracy`,
`exact_value_match_accuracy`, `tool_argument_name_accuracy`,
`tool_argument_value_accuracy`, `schema_valid_but_wrong_value_rate`,
`correct_value_wrong_schema_rate`, `fake_tool_result_claim_rate`, and
`over_refusal_structured_rate`.

Required controls include:

```text
STATIC_JSON_CONTROL
COPY_SCHEMA_CONTROL
TEMPLATE_ONLY_CONTROL
RANDOM_JSON_CONTROL
RANDOM_VALUE_CONTROL
EXTRA_FIELD_CONTROL
MISSING_FIELD_CONTROL
COPY_INJECTED_TEXT_CONTROL
INJECTED_ARGUMENT_CONTROL
ALWAYS_REFUSE_JSON_CONTROL
```

134 remains a text-generation research harness. It must not add public API,
modify runtime/tool execution, call external tools, add SDK exports, or touch
docs/product or docs/releases.

## Boundary

133 is planning only. 134 is planned as research repair only. It is not
GPT-like assistant readiness, not open-domain assistant readiness, not
production chat, not public API, not deployment readiness, not safety alignment,
and not Hungarian assistant readiness.
