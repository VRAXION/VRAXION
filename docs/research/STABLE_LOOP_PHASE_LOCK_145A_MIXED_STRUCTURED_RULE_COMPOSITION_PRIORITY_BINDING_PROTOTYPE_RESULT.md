# STABLE_LOOP_PHASE_LOCK_145A_MIXED_STRUCTURED_RULE_COMPOSITION_PRIORITY_BINDING_PROTOTYPE Result

145A records the executable prototype result for mixed structured-rule composition priority binding.

Boundary: 145A is constrained helper/backend evidence only: mixed structured-rule composition with explicit priority over block types only, not natural-language rule reasoning, not open-ended arbitration, not GPT-like/open-domain/broad assistant capability, not production/public API/deployment/safety readiness, and not architecture superiority.

The new manifest-gated decoder under test is:

```text
deterministic_pocket_gated_mixed_structured_rule_composition_binding_decoder
```

## Expected Positive Meaning

The expected positive route is:

```text
decision = mixed_structured_rule_composition_priority_binding_prototype_positive
verdict = INSTNCT_MIXED_STRUCTURED_RULE_COMPOSITION_PRIORITY_BINDING_PROTOTYPE_POSITIVE
next = 145H_MIXED_STRUCTURED_RULE_COMPOSITION_PRIORITY_BINDING_SCALE_CONFIRM
```

A positive result means the new manifest-gated decoder can:

```text
multiple canonical structured rule blocks
-> derive one candidate pocket per valid block
-> apply explicit priority over block types
-> derive final selected pocket id
-> reuse existing static marker binding
-> reuse same-line value extraction
-> emit ANSWER=E<value>
```

## Important Controls

145A separates semantic invalid blocks from structural invalid prompts.

Semantic invalid high-priority blocks must fall through to lower-priority valid blocks. Structural invalid prompts must fallback as a whole.

Required adversarial controls include:

```text
SEMANTIC_INVALID_HIGH_PRIORITY_BLOCK_FALLTHROUGH_CONTROL
STRUCTURAL_INVALID_PROMPT_NO_FALLTHROUGH_CONTROL
PRIORITY_POCKET_ORACLE_CONTROL
SAME_BLOCKS_DIFFERENT_PRIORITY
LEGACY_STRUCTURED_RULE_METADATA_REGRESSION_CONTROL
LEGACY_SELECTED_POCKET_BINDING_REGRESSION_CONTROL
```

The `SAME_BLOCKS_DIFFERENT_PRIORITY` control must use the same block contents with distinct block candidates:

```text
quorum -> pocket_a
recency -> pocket_b
tie_break -> pocket_c
```

Changing only `priority=` must change the final selected pocket.

## Non-Claims

145A does not prove natural-language rule reasoning, open-ended arbitration, GPT-like/open-domain capability, production readiness, or architecture superiority. It is a constrained helper/backend prototype for canonical mixed structured-rule composition with explicit priority over block types.
