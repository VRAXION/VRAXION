# D44 To D45 Home Catchup

Date: 2026-05-29

The home repo was dirty on `S:/Git/VRAXION`, branch `anchorweave-awft001-training-runner`, with unrelated AnchorWeave and night-marathon files. To avoid merging stale local state into D45, D45 was implemented in a clean worktree:

```text
S:/Git/VRAXION_D45
branch: codex/d45-robust-support-policy-prototype
base: origin/main
```

`origin/main` contains D44 through D44G. D44G result:

```text
decision = ipf_breaks_under_adversarial_support
verdict = D44G_ADVERSARIAL_SUPPORT_BREAKPOINT
next = D45_ROBUST_SUPPORT_POLICY_PROTOTYPE
```

D44G interpretation:

```text
IPF/ECF works as a directional evidence field in normal, broad, factorised symbolic primitive spaces.
It breaks when repeated support is correlated or adversarial, because non-independent evidence can pull the field toward distractors.
```

Local D44G smoke artifacts were not present in the new worktree. A D44G rerun was started, but stopped because the older D44G runner did not emit continuous `progress.jsonl` during long compute. D45 therefore writes a D44G upstream manifest from the checked-in D44G docs plus the known D44G summary.

Boundary:

```text
D45 is a controlled symbolic robust-support policy probe only.
It does not prove raw visual Raven reasoning, Raven solved, DNA/genome success, AGI, consciousness, architecture superiority, or that intelligence is literally a force.
```

