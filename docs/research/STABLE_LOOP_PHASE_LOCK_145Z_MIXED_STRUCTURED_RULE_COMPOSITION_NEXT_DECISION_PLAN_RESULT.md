# STABLE_LOOP_PHASE_LOCK_145Z_MIXED_STRUCTURED_RULE_COMPOSITION_NEXT_DECISION_PLAN Result

145Z is the planning-only next-decision result after 145H scale-confirmed mixed structured-rule composition with explicit priority over block types.

Boundary: 145Z is constrained helper/backend evidence only. It decides the next model-facing bridge but does not prove natural-language reasoning, not open-ended arbitration, not GPT-like/Gemma-like capability, not production readiness, and not architecture superiority.

## Expected Decision

```text
decision = trainable_structured_reasoning_distillation_bridge_plan_recommended
selected_option = trainable_structured_reasoning_distillation_bridge_plan
next = 146A_TRAINABLE_STRUCTURED_REASONING_DISTILLATION_BRIDGE_PROTOTYPE
```

## Why This Direction

145H confirmed the current deterministic helper scaffold at scale:

```text
multiple canonical structured rule blocks
-> per-block candidate derivation
-> explicit priority over block types
-> final selected pocket
-> existing marker binding
-> same-line value extraction
```

The next gap is no longer another helper-engine rule extension. The next gap is whether the confirmed scaffold can produce a controlled curriculum for a trainable/model-facing task without hidden winner or answer shortcuts.

## Required 145Z Evidence

145Z consumes exact 145H evidence:

```text
main_eval_rows = 9600
end_to_end_answer_accuracy = 1.0
positive_composition_subset_accuracy = 1.0
fallback_control_subset_accuracy = 1.0
final_selected_pocket_derivation_accuracy = 1.0
selected_pocket_to_marker_binding_accuracy = 1.0
priority_pocket_oracle_rejection_rate = 1.0
rule_composition_ablation_accuracy = 0.0
shared_helper_no_change_since_145a = true
legacy_structured_rule_metadata_regression_passed = true
legacy_selected_pocket_binding_regression_passed = true
deterministic_replay_passed = true
```

## Target 146A

The selected next milestone is:

```text
146A_TRAINABLE_STRUCTURED_REASONING_DISTILLATION_BRIDGE_PROTOTYPE
```

146A should generate controlled supervised curriculum files from the confirmed scaffold, then train/evaluate a small model-facing predictor on canonical structured inputs. It should not start with free-form natural-language rule parsing.

Target 146A must include:

```text
curriculum_train.jsonl
curriculum_validation.jsonl
curriculum_test.jsonl
curriculum_ood_test.jsonl
teacher_trace_manifest.json
training_config.json
evaluation_report.json
oracle_shortcut_audit.json
decision.json
summary.json
report.md
```

Target 146A metrics include:

```text
teacher_label_reproduction_accuracy
selected_pocket_prediction_accuracy
final_value_prediction_accuracy
heldout_template_accuracy
ood_composition_accuracy
oracle_ablation_accuracy
shortcut_scanner_violation_count
train_validation_leakage_count
test_template_overlap_rate
deterministic_replay_passed
```

## Anti-Oracle Guard

Model-facing inputs must forbid:

```text
selected_pocket_id
winner=pocket_*
final_selected
derived_selected
answer value
gold value
target value
resolved output
expected output
teacher trace fields in input
per-row oracle metadata
```

Teacher traces may exist as artifacts, but not as model input fields.

## Interpretation

145Z is still only a planning decision. A future positive 146A would prove limited supervised imitation of the structured scaffold under controlled inputs. It would not prove natural-language reasoning, open-ended arbitration, GPT-like/Gemma-like capability, production readiness, or architecture superiority.
