# D126 Adversarial Template Overlap Repair Prototype with Gated Multi-Correction Branch Contract

Purpose: execute the first adapter-only adversarial-template overlap repair prototype with both a standard weighted-sum branch and an explicitly guarded gated multi-correction branch informed by D126X.

Boundary: D126 is controlled symbolic adversarial-template overlap repair prototype only. It preserves the symbolic formula solver, dense baseline, protected symbolic router, protected components, recurrent base paths, and D102 8% light sparse mask. It performs no natural-language pretraining, introduces no tokenizer or next-token objective, uses no raw text corpus or raw Raven tasks, and does not train a Gemma-class model.

Upstream: D125 must replay or restore as d125_adversarial_template_frontier_mapped with d126_ready=true, true_route_uncertainty_under_template_grammar_near_collision, template_near_collision as worst collision, shortcut baseline sanity, preservation, fallback_rows=0, and failed_jobs=[]. D126X must replay or restore as d126x_gated_multi_correction_probe_positive with main_d126_replaced=false and gated branch recommendation intact.

Trainable surfaces: only halting_head_adapter_delta, route_head_adapter_delta, and calibration_scalar_adapter_delta are trainable. recurrent_state_adapter, symbolic solver, dense baseline, protected symbolic router, protected components, base recurrent path, base heads, sparse mask, and protected base paths remain frozen.

Repair branches: D126 compares D126_STANDARD_WEIGHTED_SUM_REPAIR_FAIR against D126_GATED_MULTI_CORRECTION_REPAIR_FAIR plus route-priority, shortcut-suppression, calibration-gated, preservation-gated, combined-gated, and reference/control arms.

Family policy: TEMPLATE_NEAR_COLLISION_FAMILY and GRAMMAR_NEAR_COLLISION_FAMILY may become guarded_low_weight only; adversarial-template overlap, same/different surface route, order perturbation, and binding-shadow families remain reference-only and excluded from healthy claims.

Positive gate: D126 passes only if both upstream handoffs validate or restore, scale is not reduced, adapter-only repair executes, gated-vs-standard branch comparison completes, adversarial failure and route uncertainty improve, gated branch wins without shortcut or preservation regression, guarded candidate policies hold, reference-only policies hold, preservation/sparse/Rust/D68/top1/leak gates hold, fallback_rows=0, failed_jobs=[], and report/metric crosschecks pass.

Decision target: d126_adversarial_template_repair_prototype_confirmed_gated_branch -> D127_ADVERSARIAL_TEMPLATE_REPAIR_SCALE_CONFIRM_WITH_GATED_BRANCH when the gated branch wins and all gates pass.
