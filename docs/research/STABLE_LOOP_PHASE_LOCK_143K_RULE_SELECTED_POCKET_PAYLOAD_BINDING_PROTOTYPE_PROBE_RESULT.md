# STABLE_LOOP_PHASE_LOCK_143K_RULE_SELECTED_POCKET_PAYLOAD_BINDING_PROTOTYPE_PROBE Result

143K records the first executable helper primitive prototype after 143J.

Boundary: constrained helper/backend evidence only. 143K positive proves prompt-visible selected-pocket binding only. It is not rule metadata reasoning, not open-ended arbitration, not GPT-like/open-domain/broad assistant capability, not production/public API/deployment/safety readiness, and not architecture superiority.

## Expected Result

```text
decision = rule_selected_pocket_payload_binding_prototype_positive
verdict = INSTNCT_RULE_SELECTED_POCKET_PAYLOAD_BINDING_PROTOTYPE_POSITIVE
next = 143P_RULE_SELECTED_POCKET_PAYLOAD_BINDING_SCALE_CONFIRM
```

The helper extension is deliberately narrow. It is active only when an INSTNCT manifest uses:

```text
deterministic_pocket_gated_rule_selected_pocket_binding_decoder
```

Old manifests must continue to show the old behavior:

```text
resolved final marker present -> old marker readout works
no resolved final marker with FINAL_MARKERS -> fallback
ABC static marker manifest -> first marker scan behavior
old decoder with winner label -> no selected-pocket binding activation
```

## Required Evidence

The result artifacts must include:

```text
shared_helper_diff_audit.json
helper_request_audit.json
static_manifest_integrity_report.json
legacy_manifest_regression_report.json
winner_label_control_report.json
pocket_marker_order_permutation_report.json
prompt_scanner_report.json
aggregate_metrics.json
decision.json
summary.json
report.md
```

The positive gate requires:

```text
winner_label_parse_accuracy >= 0.70
selected_pocket_to_marker_binding_accuracy >= 0.70
pocket_marker_order_permutation_accuracy >= 0.70
all_marker_order_permutations_covered = true
all_winner_labels_covered = true
first_prompt_marker_shortcut_rate = 0.0
ambiguous_winner_label_rejection_rate >= 0.95
ambiguous_winner_first_label_rate = 0.0
ambiguous_winner_last_label_rate = 0.0
missing_winner_label_fallback_rate >= 0.95
missing_winner_first_prompt_marker_rate = 0.0
same_values_different_winner_accuracy >= 0.70
same_winner_different_values_accuracy >= 0.70
main_pocket_writeback_rate >= 0.80
closed_pocket_ablation_accuracy <= 0.15
visible_bypass_violation_rate = 0.0
noisy_distractor_violation_rate = 0.0
direct_pocket_value_marker_rate = 0.0
resolved_final_marker_rate = 0.0
per_row_manifest_switch_rate = 0.0
per_row_payload_marker_switch_rate = 0.0
helper_request_forbidden_metadata_count = 0
selected_pocket_id_not_in_request_metadata = true
winner_label_not_in_request_metadata = true
deterministic_replay_passed = true
legacy_manifest_regression_passed = true
```

The positive interpretation is limited to prompt-visible selected-pocket binding. It does not prove rule metadata reasoning. It does not prove open-ended arbitration.

