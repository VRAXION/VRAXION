# STABLE_LOOP_PHASE_LOCK_140G_INSTNCT_POCKET_GATED_MINIMAL_MARKER_REAL_TASK_BRIDGE_PLAN Contract

140G is the artifact-only planning milestone after the positive 140F noisy-marker bridge scale confirm.

It does not create a new capability proof. It plans the next executable milestone where explicit pocket scaffolding is reduced again and the bridge moves toward minimal-marker or implicit-gate real-task-style prompts.

## Boundary

140G does not train, run new inference, call `shared_raw_generation_helper.py` for generation, mutate checkpoints, modify helper/backend/runtime/release/product surfaces, import old phase runners, start services, deploy, change root `LICENSE`, or claim GPT-like readiness.

140G must keep false:

- `reasoning_restored`
- `raw_assistant_capability_restored`
- `structured_tool_capability_restored`
- `gpt_like_readiness_claimed`
- `open_domain_assistant_readiness_claimed`
- `production_chat_claimed`
- `public_api_claimed`
- `deployment_readiness_claimed`
- `safety_alignment_claimed`

## Required Upstream

140G requires 140F:

```text
decision = instnct_pocket_gated_noisy_marker_bridge_scale_confirmed
next = 140G_INSTNCT_POCKET_GATED_MINIMAL_MARKER_REAL_TASK_BRIDGE_PLAN
```

The upstream 140F profile must show scaled noisy/reduced-marker bridge stability: high main accuracy, high pocket writeback, closed-pocket ablation failure, high ablation delta, low direct `POCKET_VALUE=` rate, every seed passed, controls failed, and deterministic replay passed.

## Required Plan Output

The generated 140H plan must target:

```text
140H_INSTNCT_POCKET_GATED_MINIMAL_MARKER_REAL_TASK_BRIDGE_PROBE
```

The 140H plan must require:

- minimal marker or implicit gate
- natural-ish task text as the primary carrier
- explicit `POCKET_VALUE=` forbidden in main eval except controls
- explicit `POCKET_*` tokens strongly reduced
- visible wrong values retained
- noisy distractors retained
- closed-pocket ablation failure
- mutation selection still preferring the correct open-pocket config
- helper-only final eval
- expected-output canary
- AST shortcut scan
- generated text before scoring
- no expected/scorer metadata in helper requests
- deterministic replay

Recommended 140H positive gates include:

- main answer value accuracy at least `0.70`
- main pocket writeback at least `0.80`
- ablation answer value accuracy at most `0.15`
- ablation delta at least `0.45`
- explicit pocket token row rate at most `0.20`
- direct `POCKET_VALUE=` row rate at most `0.02`
- implicit or minimal gate row rate at least `0.70`
- visible bypass violation rate `0.0`
- noisy distractor violation rate `0.0`

## Decision

Expected complete decision:

```text
decision = minimal_marker_real_task_bridge_plan_complete
next = 140H_INSTNCT_POCKET_GATED_MINIMAL_MARKER_REAL_TASK_BRIDGE_PROBE
```

This is planning-only. It is not GPT-like readiness, not broad assistant capability, not production readiness, not public API readiness, not deployment readiness, and not safety alignment.
