# D64U Redefine IPF Diagnostic Claim And Set Aggregation Plan Result

## Decision

```text
decision = ipf_diagnostic_claim_redefined_set_aggregation_ready
verdict = D64U_IPF_DIAGNOSTIC_CLAIM_REDEFINED_SET_AGGREGATION_READY
next = D65_SET_INVARIANT_IPF_AGGREGATION_PROTOTYPE
failed_jobs = []
```

## Supported Claims

```text
Rust sparse IPF diagnostic layer is controller-useful in the controlled task.
Clean D63 proxy flags were not used as Rust estimator inputs in D64.
Candidate identity dependence is not confirmed.
Temporal order/trajectory is not required in the current task.
Set-invariant support/evidence aggregation is the best next D65 hypothesis.
```

## Rejected Or Unconfirmed Claims

```text
candidate score-vector identity is essential
support temporal order is essential
all diagnostic bits are calibrated
full IPF aggregation migrated
full VRAXION brain / raw Raven / AGI / consciousness / architecture superiority
```

## D65 Direction

D65 should not be a temporal trajectory milestone. It should directly test:

```text
D65_SET_INVARIANT_IPF_AGGREGATION_PROTOTYPE
```

Focus:

```text
support-set aggregation
score-shape aggregation
order-invariant aggregation
support coherence as set feature
counterfactual delta as set feature
```

Required controls:

```text
random score control
set shuffle no-op control
support content corruption
truth/label leak sentinel
order perturbation no-effect control
```

Artifacts:

```text
target/pilot_wave/d64u_redefine_ipf_diagnostic_claim_and_set_aggregation_plan/smoke/
```

Boundary: D64U only repairs the IPF diagnostic claim and prepares
set-invariant aggregation planning in controlled symbolic joint formula
discovery. It does not prove full VRAXION brain, raw visual Raven reasoning,
Raven solved, AGI, consciousness, DNA/genome success, architecture superiority,
or production readiness.
