# D112 Trig Periodic Repair Scale Confirm Contract

D112 scale-confirms the D111T adapter-only targeted repair for `TRIG_PERIODIC_SYMBOLIC_FAMILY` under larger trig scale, stronger phase/frequency/harmonic tails, and stronger Lane A/B/D plus held-out noninterference audits.

## Boundary

- Preserve the symbolic formula solver, dense baseline, protected symbolic router, protected components, and the D102 8% light protected sparse core.
- Keep D112 adapter-only, checkpointed, and rollback-enabled; do not introduce a new repair design.
- Do not proceed to symbolic-sequence bridge inside D112 and do not include symbolic-sequence or language-like families.
- Keep trig excluded from the healthy claim unless explicit promotion gates pass; if promotion does not pass, trig remains repair-only.
- Do not perform natural-language pretraining, Gemma-class training, raw Raven training, full-core repair, or production/AGI claims.

## Required execution

D112 must audit or restore D111T, record `d111t_upstream_manifest.json`, execute the unreduced requested scale, replay the D111T repair baseline, run repair-scale validation with the phase-aware recurrent-state adapter and calibration-margin regularizer, evaluate trig scale repair metrics, evaluate Lane A/B/D and held-out preservation, run shortcut/leak sentinels, and write all required artifacts under `target/pilot_wave/d112_trig_periodic_repair_scale_confirm/`.

## Positive decision criteria

The scale-confirmed repair decision is allowed only when D111T replay is valid, scale is unreduced, sparse identity and protected freezes hold, repair-scale updates execute without rollback, trig failing-case rate remains reduced by at least 20%, phase/top1-top2/harmonic/loop/mask/calibration metrics remain improved, calibration overcorrection is absent, worst-seed score remains improved, Lane A/B/D and held-out preservation gates pass, Rust path is invoked with zero fallback, leak sentinels pass, and reports cross-check.
