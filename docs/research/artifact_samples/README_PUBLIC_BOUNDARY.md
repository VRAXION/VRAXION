# Artifact samples removed from public surface

This directory is scheduled for removal from the public repository.

Research artifact sample packs, row-level ledgers, replay traces, JSONL files, exact manifests, and operational checker samples belong in the private frontier repository until explicitly reviewed for public export.

Public research pages may keep only sanitized summaries and claim boundaries.
