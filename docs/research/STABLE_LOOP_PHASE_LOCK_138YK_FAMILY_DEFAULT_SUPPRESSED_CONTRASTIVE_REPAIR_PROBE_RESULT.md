# STABLE_LOOP_PHASE_LOCK_138YK_FAMILY_DEFAULT_SUPPRESSED_CONTRASTIVE_REPAIR_PROBE Result

138YK runs the targeted family-default-suppressed contrastive repair/probe
planned by 138YJ. It starts from the 138YI target checkpoint, writes a new
target-only checkpoint under the 138YK target root, and final-evaluates through
`scripts/probes/shared_raw_generation_helper.py` only.

The required artifacts include `family_default_value_bank.json`,
`hard_negative_default_rows.jsonl`, `family_default_suppression_metrics.json`,
`raw_generation_trace.jsonl`, `scoring_results.jsonl`,
`control_arm_report.json`, `determinism_replay_report.json`, `decision.json`,
`summary.json`, and `report.md`.

The positive route is
`family_default_suppressed_contrastive_repair_positive ->
139YK_FAMILY_DEFAULT_SUPPRESSED_CONTRASTIVE_SCALE_CONFIRM`. Clean negatives are
valid and route to the appropriate forensic or redesign milestone, including
`hard_negative_default_failure`,
`family_default_shortcut_persists`, `high_frequency_value_replay_detected`,
`parrot_trap_copy_shortcut_detected`, `stale_chat_rollout_failure`,
`namespace_rollout_failure`, and
`nondeterministic_family_default_suppressed_probe`.

Raw assistant capability remains quarantined. Structured/tool capability remains
invalidated. The result is not GPT-like readiness, not open-domain assistant
readiness, not production chat, not public API, not deployment readiness, and
not safety alignment.

