# STABLE_LOOP_PHASE_LOCK_138YJ_FAMILY_DEFAULT_SUPPRESSED_CONTRASTIVE_OBJECTIVE_PLAN Contract

138YJ is artifact-only, planning-only objective redesign after 138YD.

Required upstream route:

```text
138YD decision = family_default_shortcut_analysis_complete
138YD root_cause = contrastive_objective_too_weak
138YD next = 138YJ_FAMILY_DEFAULT_SUPPRESSED_CONTRASTIVE_OBJECTIVE_PLAN
```

138YJ must design `138YK_FAMILY_DEFAULT_SUPPRESSED_CONTRASTIVE_REPAIR_PROBE`. The core requirement is that family-default wrong values become explicit hard negatives and same-family contrast groups must produce distinct correct values.

138YJ does not train, repair, run inference, call `shared_raw_generation_helper.py`, run torch forward passes, mutate checkpoints, modify helper/backend code, import old runners, delete or consolidate files, start services, deploy, modify runtime/service/deploy/product/release surfaces, modify `docs/product`, modify `docs/releases`, modify SDK exports, or change root `LICENSE`.

The 138YK plan must require helper-only final eval, generated text before scoring, expected-output canary, AST shortcut scan, helper provenance verification, checkpoint hash verification, leakage audit, scorer controls, deterministic replay, source checkpoint unchanged, target checkpoint under `target/` only, no expected/scorer metadata in helper requests, family default value bank, hard negative default rows, and clean-negative routes.

Qwen-style concepts such as Scout-First Laziness and Missing Intra-Family Variance may be used only as planning hypotheses. Output-head prior, hidden-state carrier, grower/scout behavior, and topological inhibition remain `diagnostic_gap` unless future instrumentation explicitly measures them.

Raw assistant capability remains quarantined. Structured/tool capability remains invalidated. This is not GPT-like readiness, not open-domain assistant readiness, not production chat, not public API, not deployment readiness, and not safety alignment.
