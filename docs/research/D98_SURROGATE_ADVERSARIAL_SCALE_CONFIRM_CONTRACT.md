# D98 Surrogate Adversarial Scale Confirm Contract

## Purpose

D98 adversarially scale-confirms the D97 best fair surrogate, `SURROGATE_SMALL_MLP_FAIR`, under larger scale, stronger OOD/stress tails, feature noise/dropout/corruption, calibration pressure, adversarial controls, and leak/shortcut sentinels while preserving top1 guard, D68 protection, safety margins, Rust sparse invocation, `fallback_rows=0`, and visible `failed_jobs=[]`.

## Boundary

D98 is controlled symbolic ECF/IPF joint formula discovery only. The formula solver remains symbolic. D98 does not claim a full VRAXION brain, raw visual Raven, Raven solved, AGI, consciousness, DNA/genome success, production readiness, or architecture superiority.

## Phase 0 upstream audit

The runner must verify the current branch/HEAD, check requested D97 commit `082081517b755bc57b1dd271b53435b469f06c26`, verify `target/pilot_wave/d97_mechanism_feature_audit_and_surrogate_training_prototype/`, validate D97 decision `d97_surrogate_training_prototype_confirmed` with next `D98_SURROGATE_ADVERSARIAL_SCALE_CONFIRM`, restore/rerun D97 if required, and write `d97_upstream_manifest.json`. D98 must not silently assume D97 was pushed.

## D97 handoff

D97 confirmed `SURROGATE_SMALL_MLP_FAIR` with train accuracy `0.9958`, test accuracy `0.9948`, OOD accuracy `0.9924`, stress accuracy `0.9916`, min-seed accuracy `0.9914`, overfit gap `0.01`, low-cost/OOD/top1 ambiguity tail score `0.744`, min-seed tail score `0.740`, combined OOD/joint-boundary breakpoint `0.756`, min-seed combined OOD/joint-boundary breakpoint `0.753`, top1 guard preserved, D68 preservation `1.0`, truth/oracle/Rust gates passing, `fallback_rows=0`, and `failed_jobs=[]`.

## Scale and stress settings

Requested scale is seeds `19001,19002,19003,19004,19005,19006,19007,19008,19009,19010,19011,19012` with rows `train/test/ood=540` per seed/regime/split. Requested stress extension is stress seeds `19101,19102,19103,19104,19105,19106`, stress rows `train/test/ood=720`, and stress modes `combined_low_cost_ood_top1_ambiguity_tail`, `boundary_thin_margin`, `ood_support_shift_tail`, `joint_required_ambiguous_top1`, `low_cost_pressure_tail`, `external_required_tail`, `correlated_echo_distractor_tail`, `adversarial_counter_tail`, `indistinguishable_abstain_tail`, `feature_noise_tail`, `feature_dropout_tail`, `calibration_pressure_tail`, `mixed_tail_compound`, and `worst_seed_replay_tail`. Any scale reduction must be recorded and must not overclaim.

## Feature and target constraints

Fair surrogate inputs may use only D97-approved inference-time non-truth symbolic/proxy features: top1/top2 scores and gap, normalized support entropy, support dispersion, boundary distance estimate, OOD shift proxy, low-cost pressure score, joint evidence pressure proxy, external requirement proxy, abstain risk proxy, confidence risk proxy, support count estimate, and deterministic non-label symbolic structural features. Fair arms must not use truth labels, oracle route labels, support-regime labels, row IDs, seed IDs, Python hashes, file order, artifact indexes, filenames, object IDs, `repr(row)`, generated answer labels, post-hoc correctness labels, or truth/regime/route-equivalent lookup features. The teacher target remains the validated symbolic router decision generated from inference-available signals only.

## Tracks and arms

D98 includes D97 replay, surrogate scale replay, adversarial scale/OOD/stress/min-seed eval, low-cost/OOD/top1 tail scale, combined OOD/joint carryover, feature noise/dropout/corruption/ablation audits, calibration and false-positive pressure audits, top1/D68/safety/oracle/support audits, label/regime/row/hash/file-order/seed sentinels, split integrity, overfit/memorization, feature-importance stability, negative controls, truth/oracle isolation, Rust invocation, report/schema crosscheck, and deterministic replay. Arms include symbolic replay/reference arms, fair surrogate scale arms, feature perturbation arms, calibration arms, top1 controls, negative router controls, leak/shortcut sentinels, concrete oracle reference-only, and truth-leak sentinel reference-only.

## Required reports

Artifacts are written under `target/pilot_wave/d98_surrogate_adversarial_scale_confirm/` and must include `d97_upstream_manifest.json`, all D98 scale/adversarial/robustness/calibration/guard/safety/sentinel/control/provenance reports, `aggregate_metrics.json`, `decision.json`, `summary.json`, and `report.md`.

## Positive gate

D98 passes only if D97 handoff/replay is valid, requested and actual scale are recorded with `scale_reduced=false`, all stress modes execute, the best fair surrogate meets scale accuracy/min-seed/worst-seed/overfit/safety/top1/D68/truth/oracle/support gates, targeted low-cost/OOD/top1 tail and combined OOD/joint-boundary carryover are preserved, feature noise/dropout/corruption/calibration/failure-tail gates pass, leak/shortcut sentinels collapse, no forbidden features or split contamination appear, controls remain worse, deterministic replay/schema/metric crosscheck pass, Rust path is invoked, `fallback_rows=0`, and `failed_jobs=[]`.

## Decisions

- Passing adversarial scale confirmation: `decision=d98_surrogate_adversarial_scale_confirmed`, `next=D99_RECURRENT_ROUTING_MICROCIRCUIT_PROTOTYPE`.
- Tail/worst-seed failure: `decision=d98_surrogate_tail_risk_detected`, `next=D98T_SURROGATE_TAIL_RISK_REPAIR`.
- Feature robustness failure: `decision=d98_surrogate_feature_robustness_failure`, `next=D98F_FEATURE_ROBUSTNESS_REPAIR`.
- Calibration failure: `decision=d98_surrogate_calibration_failure`, `next=D98C_CALIBRATION_REPAIR`.
- Top1 guard violation: `decision=top1_guard_invariant_violation`, `next=D98G_TOP1_GUARD_REPAIR`.
- D68 regression: `decision=d68_regression_detected`, `next=D98D_D68_REGRESSION_REPAIR`.
- Truth/oracle contamination: `decision=d98_truth_leak_or_oracle_contamination_detected`, `next=D98L_TRUTH_LEAK_REPAIR`.
- Shortcut memorization: `decision=d98_shortcut_memorization_detected`, `next=D98H_SHORTCUT_MEMORIZATION_REPAIR`.
- Split contamination: `decision=d98_split_contamination_detected`, `next=D98S_SPLIT_INTEGRITY_REPAIR`.
- Rust fallback: `decision=d98_rust_fallback_detected`, `next=D98R_RUST_PATH_REPAIR`.
- Reporting inconsistency: `decision=d98_invalid_metric_or_report_inconsistency`, `next=D98_REPORTING_REPAIR`.
- Incomplete run: `decision=d98_invalid_or_incomplete_run`, `next=D98_RETRY_WITH_FULL_AUDIT`.
