# STABLE_LOOP_PHASE_LOCK_144H_STRUCTURED_RULE_METADATA_TO_SELECTED_POCKET_BINDING_SCALE_CONFIRM RESULT

144H records the scale-confirm result for the structured metadata bridge introduced by 144B:

```text
canonical structured rule metadata
-> selected pocket id derivation
-> existing selected-pocket marker binding
-> same-line value extraction
```

The decoder under scale confirm is:

```text
deterministic_pocket_gated_structured_rule_metadata_binding_decoder
```

Expected positive route:

```text
decision = structured_rule_metadata_to_selected_pocket_binding_scale_confirmed
verdict = INSTNCT_STRUCTURED_RULE_METADATA_TO_SELECTED_POCKET_BINDING_SCALE_CONFIRMED
next = 144Z_STRUCTURED_RULE_METADATA_BINDING_NEXT_DECISION_PLAN
```

Boundary: 144H is constrained helper/backend evidence only and structured rule metadata to selected-pocket binding only; it is not natural-language rule reasoning, not open-ended arbitration, not GPT-like/open-domain/broad assistant capability, not production/public API/deployment/safety readiness, and not architecture superiority.

## Result Meaning

A positive 144H confirms that the 144B structured rule metadata primitive is stable at larger coverage. It confirms deterministic parsing and derivation for canonical quorum, recency, tie-break, and hierarchy-combiner fixtures, and it confirms that the derived selected pocket still flows through the existing static marker and same-line value extraction path.

The result does not prove natural-language rule reasoning. Hierarchy remains a combiner over precomputed sub-rule winner fields. The result also does not prove open-ended arbitration, GPT-like/open-domain/broad assistant capability, production/public API/deployment/safety readiness, or architecture superiority.

Important scale controls include:

```text
clear quorum winner ignores irrelevant tie_break_order
wrong-family extra key rejection
duplicate keys rejection
malformed separators rejection
missing keys rejection
invalid pocket ids rejection
hierarchy stale rejection priority
same template opposite rule winner
same values different rule
same rule different values
legacy selected-pocket binding regression
legacy 143W binding regression
```

The main reports are written under:

```text
target/pilot_wave/stable_loop_phase_lock_144h_structured_rule_metadata_to_selected_pocket_binding_scale_confirm/smoke
```

Required machine-readable artifacts include `aggregate_metrics.json`, `decision.json`, `summary.json`, `per_seed_gate_report.json`, `per_family_gate_report.json`, helper no-change audits, prompt scanner reports, and per-family rule reports.
