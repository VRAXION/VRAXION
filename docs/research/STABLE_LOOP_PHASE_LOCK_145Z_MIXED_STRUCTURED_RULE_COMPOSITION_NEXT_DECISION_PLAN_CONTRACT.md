# STABLE_LOOP_PHASE_LOCK_145Z_MIXED_STRUCTURED_RULE_COMPOSITION_NEXT_DECISION_PLAN Contract

145Z is the planning-only next-decision milestone after the positive 145H mixed structured-rule composition scale confirm.

Boundary: 145Z is constrained helper/backend evidence only. It decides the next model-facing bridge but does not prove natural-language reasoning, not open-ended arbitration, not GPT-like/Gemma-like capability, not production readiness, and not architecture superiority.

## Expected Route

```text
decision = trainable_structured_reasoning_distillation_bridge_plan_recommended
selected_option = trainable_structured_reasoning_distillation_bridge_plan
next = 146A_TRAINABLE_STRUCTURED_REASONING_DISTILLATION_BRIDGE_PROTOTYPE
```

## Scope

145Z is artifact-only. It must not call `raw_generate`, import `shared_raw_generation_helper`, modify helper/backend/runtime/request-key surfaces, train, mutate checkpoints, or claim broad capability.

Generated outputs must stay under:

```text
target/pilot_wave/stable_loop_phase_lock_145z_mixed_structured_rule_composition_next_decision_plan/
```

## Upstream Evidence

145Z requires exact positive 145H evidence:

```text
decision = mixed_structured_rule_composition_priority_binding_scale_confirmed
verdict = INSTNCT_MIXED_STRUCTURED_RULE_COMPOSITION_PRIORITY_BINDING_SCALE_CONFIRMED
next = 145Z_MIXED_STRUCTURED_RULE_COMPOSITION_NEXT_DECISION_PLAN
main_eval_rows = 9600
end_to_end_answer_accuracy = 1.0
positive_composition_subset_accuracy = 1.0
fallback_control_subset_accuracy = 1.0
final_selected_pocket_derivation_accuracy = 1.0
selected_pocket_to_marker_binding_accuracy = 1.0
priority_pocket_oracle_rejection_rate = 1.0
rule_composition_ablation_accuracy = 0.0
shared_helper_no_change_since_145a = true
legacy_structured_rule_metadata_regression_passed = true
legacy_selected_pocket_binding_regression_passed = true
deterministic_replay_passed = true
```

## Decision

145Z compares:

```text
trainable_structured_reasoning_distillation_bridge_plan
structured_helper_engine_extension_plan
language_interface_wrapper_around_helper_plan
stop_at_mixed_structured_rule_helper_primitive
```

The selected option must be:

```text
trainable_structured_reasoning_distillation_bridge_plan
```

Reason: 143W, 144H, and 145H scale-confirmed the structured helper scaffold layers. The next honest bridge is a controlled trainable/model-facing distillation task, not another deterministic rule-engine feature.

## Required Artifacts

145Z must write:

```text
queue.json
progress.jsonl
analysis_config.json
upstream_145h_manifest.json
evidence_chain_summary.json
structured_helper_stack_state_report.json
model_facing_bridge_gap_analysis.json
next_decision_matrix.json
target_146a_milestone_plan.json
anti_oracle_requirements.json
risk_register.json
decision.json
summary.json
report.md
```

`model_facing_bridge_gap_analysis.json` must state:

```text
structured_helper_stack_scale_confirmed = true
trainable_model_internalization_untested = true
natural_language_rule_reasoning_untested = true
open_ended_arbitration_claimed = false
gpt_like_or_gemma_like_capability_claimed = false
```

## Target 146A

`target_146a_milestone_plan.json` must be implementation-ready for:

```text
146A_TRAINABLE_STRUCTURED_REASONING_DISTILLATION_BRIDGE_PROTOTYPE
```

146A intended primitive:

```text
confirmed structured helper scaffold
-> generate controlled supervised curriculum
-> train/evaluate a small model-facing predictor on LM-compatible inputs
-> test whether it can imitate selected-pocket/value outputs without hidden oracle shortcuts
```

146A must start with canonical structured prompts, not free-form natural language.

Required 146A outputs include curriculum train/validation/test/OOD JSONL files, teacher trace manifest, training config, evaluation report, oracle shortcut audit, decision, summary, and report.

Required 146A metrics include teacher label reproduction, selected-pocket prediction, final-value prediction, heldout-template accuracy, OOD composition accuracy, oracle ablation accuracy, shortcut scanner violations, train/validation leakage, test template overlap, and deterministic replay.

146A model-facing inputs must forbid:

```text
selected_pocket_id
winner=pocket_*
final_selected
derived_selected
answer value
gold value
target value
resolved output
expected output
teacher trace fields in input
per-row oracle metadata
```

## Claim Limit

A positive 145Z is only a planning decision. A future positive 146A would prove only limited supervised imitation of the structured scaffold under controlled inputs. It would not prove natural-language reasoning, open-ended arbitration, GPT-like/Gemma-like capability, production readiness, or architecture superiority.
