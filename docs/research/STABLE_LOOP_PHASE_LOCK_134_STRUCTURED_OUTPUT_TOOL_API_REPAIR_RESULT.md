# STABLE_LOOP_PHASE_LOCK_134_STRUCTURED_OUTPUT_TOOL_API_REPAIR_RESULT

## Result Contract

The 134 runner writes generated artifacts under:

```text
target/pilot_wave/stable_loop_phase_lock_134_structured_output_tool_api_repair/
```

The expected positive result is:

```text
STRUCTURED_OUTPUT_TOOL_API_REPAIR_POSITIVE
decision = structured_output_tool_api_repair_success
next = 135_STRUCTURED_OUTPUT_TOOL_API_REPAIR_SCALE_CONFIRM
selected_repair_target = structured_output_tool_api_first
```

The result must include:

- `structured_tool_repair_dataset.jsonl`
- `structured_tool_repair_metrics.json`
- `structured_semantics_report.json`
- `tool_api_argument_report.json`
- `structured_shortcut_report.json`
- `structured_refusal_report.json`
- prior repair preservation, retention, collapse, namespace, leakage, and boundary reports

Positive 134 requires JSON/schema semantics, exact values, tool argument
names/values, structured refusal, injection-resistant structured fields,
template-only shortcut rejection, fake tool-use rejection, controls failing,
leakage rejection, and bounded release immutability.

134 remains targeted research repair only and a text-generation harness for
structured/tool-like output only. It is not actual tool execution, not GPT-like
assistant readiness, not open-domain assistant readiness, not production chat,
not public API, not deployment readiness, not safety alignment, and not
Hungarian assistant readiness.
