# D110 Frontier Expansion Scale Confirm Contract

D110 scale-confirms the D109 adapter-only controlled frontier-expansion train-loop at larger scale. It is not a symbolic-sequence bridge, not natural-language pretraining, not Gemma-class training, and not a production-readiness or AGI claim.

## Boundary

- Preserve the symbolic formula solver, protected symbolic router, dense baseline, protected components, and 8% sparse mask.
- Trainable surfaces remain limited to `route_head_adapter`, `halting_head_adapter`, `recurrent_state_adapter`, and `calibration_scalar_adapter`.
- Keep `SYMBOLIC_SEQUENCE_ROUTING_FAMILY`, `LANGUAGE_LIKE_SYMBOLIC_COMMAND_FAMILY`, `HELDOUT_DEPTH_6_COMPOSITION_FAMILY`, and `ADVERSARIAL_FAMILY_MIX_TRANSFER_FAMILY` held out from D110 training.
- Keep `TRIG_PERIODIC_SYMBOLIC_FAMILY` in targeted repair only and excluded from the healthy claim.
- Keep `MIXED_SYMBOLIC_TRANSFER_FAMILY` provisional-normal with guarded stop gates.

## Required execution

D110 must audit or restore D109, record `d109_upstream_manifest.json`, execute the requested unreduced scale, run adapter-only checkpointed updates, audit all lanes, audit held-out rejected-family noninterference, run shortcut/leak sentinels, and write all D110 reports under `target/pilot_wave/d110_frontier_expansion_scale_confirm/`.

## Positive decision criteria

The healthy decision is `d110_frontier_expansion_scale_confirmed` only when D109 replay is valid, scale is unreduced, sparse identity is preserved at 8% light pressure, protected component modifications are zero, adapter training executes with no rollback, Lane A/B/C/D gates pass, held-out rejected families remain noninterfering, Rust path is invoked with zero fallback rows, sentinel audits pass, reports cross-check, and failed jobs are empty.
